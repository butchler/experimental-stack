export const
       GOAL_URL = '/api/goal.js',
       // Number of milliseconds to show selected cards after flipping two cards.
       SELECTION_DELAY = 2000,
       // How long to wait before showing the results screen after winning the game.
       SHOW_RESULTS_DELAY = 1000,
       // Number of items to pull from the goal API (NUM_ITEMS * 2 cards will be
       // created, one for the cue and one for the response of each item).
       NUM_ITEMS_EASY = 6,
       NUM_ITEMS_MEDIUM = 9,
       NUM_ITEMS_HARD = 18;
